name: Live Match Updater
on:
  schedule:
    - cron: '*/20 * * * *' # تشغيل تلقائي كل 20 دقيقة
  workflow_dispatch: # زر للتشغيل اليدوي للتجربة

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Requests
        run: pip install requests

      - name: Run Updater
        env:
          RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
        run: python update_data.py

    - name: Commit and Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git pull origin main --rebase # هذه الإضافة ستحل مشكلة الـ rejected
          git add api.json
          git commit -m "Update live matches" || exit 0
          git push origin main
